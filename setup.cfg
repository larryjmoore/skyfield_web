[metadata]
name = skyfield_web

[options]
packages = find:
package_dir =
    = .

[options.packages.find]
exclude =
    data*
    images*
    tests*
    venv*

[options.extras_require]
test =
    pytest
